{% extends 'base.html.twig' %}

{% block title %}Editeur{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center">Éditeur</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ editeur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ editeur.nom }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ editeur.resume }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ editeur.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-outline-secondary my-2" href="{{ path('editeur_index') }}">Retour</a>
    <a class="btn btn-outline-primary" href="{{ path('editeur_edit', {'id': editeur.id}) }}"><i class="far fa-edit"></i></a>
    {{ include('editeur/_delete_form.html.twig') }}
</div>
{% endblock %}
