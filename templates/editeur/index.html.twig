{% extends 'base.html.twig' %}

{% block title %}Editeur index{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center">Éditeur</h1>
    <table class="table table-dark table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Resume</th>
                <th>Active</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for editeur in editeurs %}
            <tr>
                <td>{{ editeur.id }}</td>
                <td>{{ editeur.nom }}</td>
                <td>{{ editeur.resume }}</td>
                <td>{{ editeur.active ? 'Yes' : 'No' }}</td>
                <td>
                    <div class="d-flex">
                        <a class="link-secondary me-2" href="{{ path('editeur_show', {'id': editeur.id}) }}"><i class="fas fa-eye"></i></a>
                        <a href="{{ path('editeur_edit', {'id': editeur.id}) }}"><i class="fas fa-edit"></i></a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a class="btn btn-outline-primary mt-2" href="{{ path('editeur_new') }}">Ajouter</a>
</div>
{% endblock %}
