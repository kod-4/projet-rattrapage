{% extends 'base.html.twig' %}

{% block title %}Edit Rencontre{% endblock %}

{% block body %}
    <div class="col-md-8 mx-auto mt-5">
        <h1 class="text-center">Modifier Rencontre</h1>
        {{ include('rencontre/_form.html.twig', {'button_label': 'Update'}) }}
        <a class="btn btn-outline-secondary mt-1" href="{{ path('rencontre_index') }}">Retour</a>
        {{ include('rencontre/_delete_form.html.twig') }}
    </div>
{% endblock %}
